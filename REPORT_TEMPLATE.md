# 周报内容模板

## 格式规范

### 分类结构
- 一、业务 & 开发
- 二、团队效能与基础建设
- （可根据实际内容增减分类）

### 写作风格
- 每条以动词开头：实现、推进、完成、修复、优化、完善、重构、接入、开发
- 一句话说清楚，不加修饰语
- 技术术语直接使用（如 vue3、elementplus 等）
- **无编号**，一条一行（编号由 OA 自动生成）

### 数据整合规则（提取 → 合并 → 汇总）
- **合并同类项**：同一项目/模块的多条提交、多个会话要聚合为 1-2 条完整句子，不要逐条照搬 commit message / 会话标题
- **去重**：内容经归一化（去标点/空格/大小写）后比对，相同只保留一条；跨来源（Git、AI 会话、任务）重复的也合并；若一条完整包含另一条，保留更长的一条
- **提炼语义**：把 `feat(collect): 文档更新按 commit 聚合汇总` 这类原始提交改写为业务叙事，如 `完善周报自动化 Skill 文档（数据源与采集器、Git 提交明细、填单示例）`
- 优先使用任务完成数据作为"完成工作"来源，Git/会话作为补充佐证
- 一个项目/子域最多保留 4 条（文档类可到 6 条），保证精简

### 四级结构（供参考示例，report.json 中不写编号）
```
一、业务 & 开发            ← 分类标题（普通段落）
  项目A                    ← 一级：项目/模块（仓库名/项目名直出）
    实现本地在线编译能力       ← 二级：具体事项
    完成登录流程改造
  项目B                    ← 一级
    推进数据报表模块迭代
二、团队效能与基础建设
  文档更新
    完善周报自动化 Skill 文档（数据源、Git 扫描、填单示例）
```

## 示例（issue：内容必须无编号，编号由 OA 自动生成）

本周完成工作
一、业务 & 开发
项目A
实现本地在线编译能力
完成登录流程改造，覆盖全端
项目B
推进数据报表模块迭代与接口联调
二、团队效能与基础建设
文档更新
完善周报自动化 Skill 文档（数据源与采集器、Git 提交明细、填单示例）
本周未完成工作及原因
/
下周工作计划
继续推进项目A 相关工作
继续推进项目B 相关工作
需要协调与帮助
/
学习和反思
本周聚焦项目A、项目B 相关工作，保持稳定推进节奏

## 数据分析要点

从采集数据中提取：
1. **具体项目名称**（仓库名 / 项目路径最后一段，直出）
2. **做了什么**（实现、推进、完成、修复、优化）
3. **技术细节**（vue3、elementplus、headers 集成等）
4. **协作事项**（配合、推动、参与）

## 注意事项

- 不要写"本周完成了xxx工作"这种废话，直接说事
- **严禁写任何序号**（`1.` / `a.` / `i.` / `一二三、`），编号由 OA 系统自动生成；只有分类标题（一、业务 & 开发）保留
- 不要加"等"字结尾（除非确实有省略）
- 可以用逗号连接相关事项
- 数据的原始层级在第 1/2 级即可（分类→项目→事项），不要堆三层以上